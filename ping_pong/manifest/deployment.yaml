apiVersion: apps/v1
kind: Deployment
metadata:
  name: pingpong-dep
  namespace: exercises
spec:
  replicas: 1
  selector:
    matchLabels:
      app: pingpong
  template:
    metadata:
      labels:
        app: pingpong
    spec:
      containers:
        - name: pingpong
          image: stevemcwin/pingpong:2.1
          env:
            - name: DB_HOST
              value: postgresql-svc
            - name: DB_PORT
              value: "4321"
            - name: DB_USER
              value: postgres
            - name: DB_PASSWORD
              value: myPassword123
            - name: DB_NAME
              value: postgres
